spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
  #这个配置会到nacos配置中心找到pay-server-dev.yaml和pay-server.yaml这两个文件
  #这里一定要注意nacos配置里的文件名一定要带上后缀yaml
  application:
    name: pay-server
  profiles:
    active: dev

